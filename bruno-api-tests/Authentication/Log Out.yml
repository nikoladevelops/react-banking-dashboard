info:
  name: Log Out
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/api/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
