info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "{{username}}",
        "password": "{{password}}"
      }
  auth: inherit

runtime:
  variables:
    - name: username
      value: testing1
    - name: password
      value: testing1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
