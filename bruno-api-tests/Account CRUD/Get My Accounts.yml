info:
  name: Get My Accounts
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
