info:
  name: Delete Account
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}/api/accounts/{{accountId}}"
  auth: inherit

runtime:
  variables:
    - name: accountId
      value: 6a1835b73551356da1e2e1c1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
