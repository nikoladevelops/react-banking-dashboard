info:
  name: Get Single User
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseURL}}/api/users/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
