info:
  name: Get Transaction By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/transactions/{{transactionId}}"
  auth: inherit

runtime:
  variables:
    - name: transactionId
      value: 6a184ce9e133f0d9d0fa4045

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
