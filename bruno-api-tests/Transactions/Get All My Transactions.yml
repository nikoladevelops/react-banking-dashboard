info:
  name: Get All My Transactions
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/transactions/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
