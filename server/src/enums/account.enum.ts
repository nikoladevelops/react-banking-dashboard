export enum AccountType {
  CHECKING = "checking",
  SAVINGS = "savings",
  CURRENT = "current",
  BUSINESS = "business",
  FREE_CURRENT = "free_current",
}

export enum AccountStatus {
  ACTIVE = "active",
  FROZEN = "frozen",
  CLOSED = "closed",
}
